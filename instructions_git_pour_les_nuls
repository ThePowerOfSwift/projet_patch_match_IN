1) si le dépôt n'est pas crée : git clone https://github.com/pouloup/projet_patch_match_IN.git
1 bis) suivre les indications de git, ie : git config --global user.email "mail" etc...

Procédur à toujours suivre :

2) git pull
3) git add -A (ajouter tout les fichiers modifiés au commit)
4) git commit (confirmation des modifications + message de commit IMPÉRATIF)
5) git push
